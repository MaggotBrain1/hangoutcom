{% extends 'base.html.twig' %}

{% block main %}

    {{ form_start(editUserForm) }}
<h1>Mon profil</h1>
<div class="container text-left">


    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.pseudo) }} :</div>
        <div class="col-6">{{ form(editUserForm.pseudo) }}</div>
    </div>

    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.name) }} :</div>
        <div class="col-6">{{ form(editUserForm.name) }}</div>
    </div>

    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.lastName) }} :</div>
        <div class="col-6">{{ form(editUserForm.lastName) }}</div>
    </div>
    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.phone) }} :</div>
        <div class="col-6">{{ form(editUserForm.phone) }}</div>
    </div>
    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.email) }} :</div>
        <div class="col-6">{{ form(editUserForm.email) }}</div>
    <div class="row no-gutters">
        <div class="col-6">{{ form_label(editUserForm.plainPassword) }} :</div>
        <div class="col-6">{{ form(editUserForm.plainPassword) }}</div>
    </div>
       {# <div class="row no-gutters">
            <div class="col-6">{{ form_label(editUserForm.confirm) }} :</div>
            <div class="col-6">{{ form(editUserForm.confirm) }}</div>
        </div>#}
    <div class="row">
        <div class="col-6">{{ form_label(editUserForm.campus) }} :</div>
        <div class="col-6">{{ form(editUserForm.campus) }}</div>
    </div>
    </div>
            <button type="submit">Enregistrer</button>
</div>

            {{ form_end(editUserForm) }}

{% endblock %}

{% block title %}
    {{ app.user.name }} | {{ app.user.lastname }}
{% endblock %}

