{% extends 'base.html.twig' %}

{% block title %}Manage City{% endblock %}

{% block main %}
    <h1 class="container text-center">Gérer les villes</h1>
    <h2 class="text-left">Filtrer les villes</h2>
    <p class="text-left">Le nom contient :</p>
    <input type="text" name="search" placeholder="HER">
    <button type="submit">Rechercher</button>

    <tr>
        <th id="campusName" scope="row" class="noBorder effetneon">Ville</th>
        <th id="campusZipCode" scope="row" class="noBorder effetneon">Code Postal</th>
        <th id="actions" scope="row" class="noBorder effetneon">Actions</th>
    </tr><br>
    {% for city in cities %}
        <tr>
            <td>{{ city.name }}</td>
        </tr>
        <tr>
            <td>{{ city.zipCode }}</td>
        </tr><br>
    {% endfor %}
{% endblock %}