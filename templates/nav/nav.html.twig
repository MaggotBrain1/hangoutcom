<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path("app_home") }}" title="Home"><img
                    src="{{ asset('img/logo/LogoHangout.jpg') }}"></a>
        <div class="collapse navbar-collapse">
            <div class="navbar-nav">
                {% if app.user %}
                    <a class="nav-link active" href="{{ path("app_profile",{'id' : app.user.id}) }}" title="Profile">Profile</a>
{#
                    <a class="nav-link active" href="{{ path("app_hangout_list") }}" title="Create">View Hangouts</a>
#}
                    <a class="nav-link active" href="{{ path("app_hangout_create",{'id' : app.user.id}) }}" title="Create">Create
                            Hangout</a>
                    <a class="nav-link active" href="{{ path("app_logout") }}" title="Home">Logout</a>
                {% else %}
                    <a class="nav-link active" href="{{ path("app_login") }}" title="Login">Login</a>
                {% endif %}
            </div>
        </div>
    </div>
{#
    {% include('fragments/headerInfo.html.twig')%}
#}

</nav>